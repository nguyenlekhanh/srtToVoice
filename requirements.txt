# Phase 0 — project skeleton
#
# The UI uses tkinter, which ships with the standard Python installation
# on Windows, so no third-party packages are required for the UI itself.
#
# Phase 1-3 (Piper TTS, audio, subtitles):
piper-tts==1.7.0
pysrt==1.1.2
pydub==0.25.1
chardet==7.6.0
pathvalidate==3.3.1
#
# Phase 4 (video upload + preview):
# av        — decodes video/audio containers (bundles FFmpeg libs as a
#             wheel; no system FFmpeg needed, source file never modified)
# sounddevice — PortAudio output for preview volume/mute control
# pillow    — converts decoded frames to images for the Tkinter canvas
# numpy     — audio sample scaling for volume/mute (av dependency too)
av==18.1.0
sounddevice==0.5.6
pillow==12.3.0
numpy==2.5.2
